The file containing the flag is called f1@g
The key is the name (abbreviated) of the algorithm that generates the keystream (make sure you don't have a newline at the end of the file!).
(Hint: How do you find a file? Use the find command).